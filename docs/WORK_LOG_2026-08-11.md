# WORK LOG — 2026-08-11

## Ringkasan
Sesi verifikasi konektivitas: audit semua menu, tombol, route, controller, JS handler — pastikan tak ada fungsi mati / tombol yang cuma refresh halaman. Sekalian sinkronisasi docs dengan semua perbaikan (kemarin + hari ini).

## Git Status
- Branch: `develop`
- Perbaikan belum di-commit (menunggu instruksi).

## Verifikasi Konektivitas (AUDIT)
- `php artisan route:list`, `php artisan migrate:status`, `php artisan test` dijalankan.
- Migrasi terakhir (kemarin) sudah `Ran`: `spp_sequences`, `terserap_sementara`.
- Semua view admin + SPP + auth + profile + dashboard + notifications di-audit: route, form action, named-route, JS `onclick/onchange/oninput/delegate`, AJAX/fetch.

## Yang Diperbaiki Hari Ini (✅)

| # | Temuan | Fix | File |
|---|--------|-----|------|
| 1 | Dashboard "My Tasks": klik No. Surat `onclick="showDetailSpp()"` → ReferenceError (fungsi cuma export di `spp.js`, tak global), modal tak terbuka | Ganti ke `data-action="detail-spp"` + `data-no-surat` (delegasi global `spp.js` sudah tangani) | `resources/views/dashboard/index.blade.php` |
| 2 | Tombol "Hapus Semua Akses": POST tanpa `@method('DELETE')` (route = DELETE → 405) + form tersarang di dalam form utama (browser drop nested form) | Tambah `@method('DELETE')`; form clear dipindah keluar dari form utama; "Simpan Akses" tetap di dalam form | `resources/views/admin/user/access_edit.blade.php` |
| 3 | "Download Template CSV" di import budget → 404 (file/folder tak ada) | Buat `storage/app/public/templates/master_budget_import_template.csv` (header + contoh baris); symlink `public/storage` sudah ada | `storage/app/public/templates/` |
| 4 | Tombol tandai-dibaca per notifikasi → halaman tampil JSON mentah `{"success":true}` | `NotificationController::markAsRead` ganti return `redirect()->back()`; dropdown `layout.js` tetap jalan (fetch tak parse body) | `app/Http/Controllers/NotificationController.php` |
| 5 | Halaman `pilih_peran` mati (tak pernah dirender; `authenticate` otomatis pilih role pertama) + backup | Hapus `resources/views/auth/pilih_peran.blade.php` + `pilih_peran.backup.blade.php`; sync docs 5.5 | `resources/views/auth/` |
| 6 | Inkonsistensi ADMIN read-only: tombol "Perbaiki" tampil untuk ADMIN tapi `update()` menolak non-maker (dead button) | Hapus bypass ADMIN di kondisi tombol (index + kelola) dan `SppController::edit()` → 403 konsisten | `resources/views/spp/index.blade.php`, `spp/kelola.blade.php`, `app/Http/Controllers/SppController.php` |
| 7 | Test pra-existing fail: `FileUploadServiceTest` pakai fake PDF MIME `application/octet-stream` ditolak `isValidMime` | Ganti `UploadedFile::fake()->create()` → `createWithContent()` dengan magic bytes valid (PDF/JPEG) | `tests/Unit/Services/FileUploadServiceTest.php` |
| 8 | Artfak compiled view `storage/framework/views/…php` tak ter-ignore | Tambah `/storage/framework/views/*.php` di `.gitignore`; hapus file | `.gitignore` |

## Docs Diperbarui (✅)
- `docs/05-role-permission-matrix.md`:
  - Matriks 5.2: Validasi SPP ADMIN `✅**` → `-`; Pencairan Dana ADMIN `✅` → `-` (special mark `✅**` dihapus).
  - Seksi 5.6 diganti "**Admin sebagai Read-Only Auditor**" + snippet kode aktual (`validateWorkflowTransition` return false; `DisburseSppRequest` cek `KASIR_PUSAT`).
  - Seksi 5.5 Multi-Role disinkron: login → role pertama aktif, switch via dropdown `/switch-role` (bukan pilih_peran).
- `docs/REVIEW_TEMUAN.md` sudah akurat (semua P0/P1 status FIXED, sisanya P2), tidak diubah.

## Test
- **193 passed, 0 failed** (427+35 assertions). Sebelumnya 192 pass + 1 fail test-file; fail itu test-bug, bukan bug produksi, sudah diperbaiki (item 7).
- Setelah perbaikan P2 tambahan: **200 passed, 0 failed** (440 assertions).

## Belum Dikerjakan (P2 — dari WORK_LOG kemarin, masih terbuka)
| Kode | Temuan |
|------|--------|
| B4 | Overspend blocking (keputusan user: skip) |
| B8 | Notifikasi `sendToRole` tak filter area/project |
| F1 | Threshold DIREKTUR pakai `total_nominal` seluruh item (konfirmasi kebijakan) |
| I1–I8 | Improvement UX |
| - | Modul UM/LPJ/Reimburse — `docs/12-um-lpj-reimburse-plan.md` |

## Perbaikan P2 Tambahan (✅ — setelah verifikasi utama)

| Kode | Temuan | Fix |
|------|--------|-----|
| F2 | MAKER/role scoped bisa ajukan SPP project/area di luar kewenangan (hanya cek UI) | Validasi server di `StoreSppRequest::withValidator` → `validateScope()`: staff-area wajib `kode_area`=session; project dibatasi jika area terdaftar di `project_area` (fallback aman utk area tak terpeta). Project-scoped: `kode_project`=session (kecuali `all`) + `kode_area` ∈ `project_area`. Global & `MANAGER_KEUANGAN` project `all` dibebaskan. |
| F4 | `SppObserver@updated` selalu log `UPDATE_SPP` (termasuk touch-only, berisiko bocor data rekening di payload) | Filter kolom relevan: `status_surat, posisi_saat_ini, total_nominal, keterangan_checker, biaya_admin` → log hanya jika salah satu berubah |
| S3 | `SystemSetting` tak dipakai runtime | 3 konsumen: (1) `ValidateSession` timeout dari `session_timeout` (menit, default 120); (2) `StoreSppRequest` max file dari `max_file_size` (KB, default 5120); (3) middleware baru `CheckMaintenance` (web group) — `maintenance_mode=true` → 503, sesi ADMIN bypass |

Test baru: `SppCreationTest` +2 (staff-area area-mismatch, project-scoped wrong project), `SppObserverTest` +2 (update tak-log utk kolom irrelevan, log utk relevan), `SystemSettingRuntimeTest` baru +3 (maintenance block/bypass/off). **200 passed, 0 failed.**

## Catatan
- Route `admin.user.access.delete_row` (DELETE) & `admin.activity.online` belum dipakai view mana pun — dibiarkan (harmless).
- Template CSV belum di-git (folder `storage/` di-ignore). Deployment perlu membuat file sendiri atau `git add -f storage/app/public/templates/master_budget_import_template.csv`.